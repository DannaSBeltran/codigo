# codigo
1. tenemos el log para extraer los datos, luego con las expresiones irregulares, sacamos los datos del log y los llamamos con el print ahi
   se genera el JSON luego se genera la grafica por medio de la libreria matplotlib
